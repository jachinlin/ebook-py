language: python
python:
  - "3.5"
  - "3.6"
  - "3.7"

# command to install dependencies
install:
  - pip install -r requirements.txt
  - pip install pytest pytest-cov codecov

# command to run tests
script:
  - python -m pytest -s --cov=./ebook

after_success:
  - codecov
